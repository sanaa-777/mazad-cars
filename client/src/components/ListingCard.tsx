import { CalendarDays, Gauge, Gavel, MapPin } from "lucide-react";
import { Link } from "wouter";
export type ListingCardData = { id: number; title: string; make: string; model: string; year: number; mileage: number; city: string; status: "draft" | "pending" | "published" | "rejected" | "sold" | "archived"; saleType: "sale" | "auction"; price: number | null; currentBid: number | null; coverImageUrl?: string | null; auctionEndsAt?: Date | number | null; isDemo?: boolean; };
const currency = new Intl.NumberFormat("ar-YE");
export function ListingCard({ listing }: { listing: ListingCardData }) { const amount = listing.saleType === "auction" ? listing.currentBid ?? listing.price : listing.price; return <Link href={`/listings/${listing.id}`} className="group overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200 transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#172b4d]/10 focus-visible:ring-2 focus-visible:ring-[#f7ad32]"><div className="relative aspect-[16/10] overflow-hidden bg-gradient-to-br from-[#243c68] to-[#101f3b]">{listing.coverImageUrl ? <img src={listing.coverImageUrl} alt={`صورة ${listing.title}`} className="h-full w-full object-cover transition duration-300 group-hover:scale-105" loading="lazy" /> : <div className="grid h-full place-items-center text-slate-300"><Gavel className="h-10 w-10" aria-hidden="true" /></div>}<span className={`absolute right-3 top-3 rounded-full px-2.5 py-1 text-xs font-extrabold text-white ${listing.saleType === "auction" ? "bg-[#d9594c]" : "bg-[#172b4d]"}`}>{listing.saleType === "auction" ? "مزاد" : "للبيع"}</span>{listing.isDemo ? <span className="absolute left-3 bottom-3 rounded-full bg-white/95 px-2.5 py-1 text-[10px] font-black text-[#9b5a11]">معاينة فقط</span> : null}</div><div className="p-4"><h2 className="line-clamp-1 text-sm font-black text-[#172b4d]">{listing.title}</h2><div className="mt-2 flex flex-wrap gap-x-3 gap-y-1 text-xs text-slate-500"><span className="inline-flex items-center gap-1"><CalendarDays className="h-3.5 w-3.5" />{listing.year}</span><span className="inline-flex items-center gap-1"><Gauge className="h-3.5 w-3.5" />{currency.format(listing.mileage)} كم</span><span className="inline-flex items-center gap-1"><MapPin className="h-3.5 w-3.5" />{listing.city}</span></div><div className="mt-4 border-t border-slate-100 pt-3"><p className="text-[11px] font-bold text-slate-400">{listing.isDemo ? "بيانات معاينة" : listing.saleType === "auction" ? "المزايدة الحالية" : "السعر المطلوب"}</p><p className="mt-0.5 text-base font-black text-[#e98716]">{listing.isDemo ? "غير متاح للبيع" : amount ? `${currency.format(amount)} ر.ي` : "السعر عند التواصل"}</p></div></div></Link>; }
