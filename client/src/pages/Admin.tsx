import { useState } from "react";
import { BadgeCheck, FileCheck2, Gavel, History, ShieldAlert, Users } from "lucide-react";
import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/_core/hooks/useAuth";
import { MarketplaceLayout } from "@/components/MarketplaceLayout";
import { currency, dateTime, decimalToNumber } from "@/lib/marketplace";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";

type Tab = "overview" | "listings" | "documents" | "auctions" | "users" | "audit";
const nav: { id: Tab; label: string; icon: typeof Gavel }[] = [
  { id: "overview", label: "نظرة عامة", icon: Gavel }, { id: "listings", label: "الإعلانات", icon: BadgeCheck }, { id: "documents", label: "التوثيق", icon: FileCheck2 }, { id: "auctions", label: "المزادات", icon: Gavel }, { id: "users", label: "المستخدمون", icon: Users }, { id: "audit", label: "السجل", icon: History },
];

export default function Admin() {
  const auth = useAuth(); const utils = trpc.useUtils(); const [tab, setTab] = useState<Tab>("overview"); const [selectedAuctionId, setSelectedAuctionId] = useState<number | null>(null);
  const enabled = auth.user?.role === "admin";
  const dashboard = trpc.marketplace.admin.dashboard.useQuery(undefined, { enabled });
  const queues = trpc.marketplace.admin.queues.useQuery(undefined, { enabled });
  const auctions = trpc.marketplace.admin.auctions.useQuery(undefined, { enabled });
  const auctionBids = trpc.marketplace.admin.auctionBids.useQuery({ auctionId: selectedAuctionId ?? 0 }, { enabled: enabled && selectedAuctionId !== null });
  const refresh = () => { utils.marketplace.admin.invalidate(); utils.marketplace.listings.invalidate(); };
  const reviewListing = trpc.marketplace.admin.reviewListing.useMutation({ onSuccess: () => { toast.success("تم تحديث حالة الإعلان"); refresh(); }, onError: (error) => toast.error(error.message) });
  const reviewDocument = trpc.marketplace.admin.reviewVerification.useMutation({ onSuccess: () => { toast.success("تم تحديث حالة الوثيقة"); refresh(); }, onError: (error) => toast.error(error.message) });
  const setUserStatus = trpc.marketplace.admin.setUserStatus.useMutation({ onSuccess: () => { toast.success("تم تحديث حالة المستخدم"); refresh(); }, onError: (error) => toast.error(error.message) });
  const closeAuction = trpc.marketplace.admin.closeAuction.useMutation({ onSuccess: () => { toast.success("تم تحديث حالة المزاد"); setSelectedAuctionId(null); refresh(); }, onError: (error) => toast.error(error.message) });

  if (auth.loading) return <DashboardLayout><div className="p-6 text-sm text-slate-500">جارٍ التحقق من الصلاحيات...</div></DashboardLayout>;
  if (auth.user?.role !== "admin") return <MarketplaceLayout title="لوحة الإدارة"><div className="rounded-3xl bg-white p-10 text-center shadow-sm ring-1 ring-slate-200"><ShieldAlert className="mx-auto h-10 w-10 text-red-600" /><h1 className="mt-3 text-xl font-black text-[#172b4d]">ليس لديك صلاحية الوصول</h1><p className="mt-2 text-sm text-slate-500">لوحة الإدارة متاحة للمشرفين المعتمدين فقط.</p></div></MarketplaceLayout>;
  const loading = dashboard.isLoading || queues.isLoading || auctions.isLoading;
  const failed = dashboard.isError || queues.isError || auctions.isError;

  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-5"><header className="rounded-3xl bg-[#172b4d] p-6 text-white"><p className="text-sm font-bold text-[#f7ad32]">منطقة إدارية محمية</p><h1 className="mt-1 text-2xl font-black">لوحة إدارة مزايد سيارات</h1><p className="mt-2 text-sm text-slate-300">إدارة المراجعات والمستخدمين وسجل العمليات من مصدر بيانات واحد.</p></header><nav className="flex gap-2 overflow-x-auto pb-1" aria-label="أقسام الإدارة">{nav.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => setTab(id)} className={`inline-flex shrink-0 items-center gap-2 rounded-xl px-4 py-2 text-sm font-black focus-visible:ring-2 focus-visible:ring-[#f7ad32] ${tab === id ? "bg-[#e98716] text-white" : "bg-white text-slate-600 ring-1 ring-slate-200"}`}><Icon className="h-4 w-4" />{label}</button>)}</nav>{loading ? <div className="grid gap-4 md:grid-cols-3"><div className="h-28 animate-pulse rounded-2xl bg-slate-200" /><div className="h-28 animate-pulse rounded-2xl bg-slate-200" /><div className="h-28 animate-pulse rounded-2xl bg-slate-200" /></div> : null}{failed ? <section className="rounded-2xl bg-red-50 p-5 text-sm text-red-700"><strong>تعذر تحميل إحدى بيانات الإدارة.</strong><Button variant="outline" className="mr-3" onClick={refresh}>إعادة المحاولة</Button></section> : null}{tab === "overview" ? <div className="grid gap-4 md:grid-cols-3"><Metric label="إعلانات بانتظار المراجعة" value={dashboard.data?.pendingListings ?? 0} icon={BadgeCheck} /><Metric label="وثائق بانتظار المراجعة" value={dashboard.data?.pendingDocuments ?? 0} icon={FileCheck2} /><Metric label="مزادات مباشرة" value={dashboard.data?.liveAuctions ?? 0} icon={Gavel} /></div> : null}{tab === "listings" ? <ListingQueue rows={queues.data?.listings ?? []} approve={(id) => reviewListing.mutate({ listingId: id, decision: "published" })} reject={(id) => reviewListing.mutate({ listingId: id, decision: "rejected", note: "يرجى مراجعة بيانات الإعلان وصوره قبل إعادة الإرسال." })} busy={reviewListing.isPending} /> : null}{tab === "documents" ? <DocumentQueue rows={queues.data?.documents ?? []} approve={(id) => reviewDocument.mutate({ documentId: id, decision: "approved" })} reject={(id) => reviewDocument.mutate({ documentId: id, decision: "rejected", note: "الوثيقة غير واضحة أو لا تطابق المتطلبات." })} busy={reviewDocument.isPending} /> : null}{tab === "auctions" ? <AuctionPanel rows={auctions.data ?? []} selectedId={selectedAuctionId} onSelect={setSelectedAuctionId} bids={auctionBids.data ?? []} loadingBids={auctionBids.isLoading} busy={closeAuction.isPending} onClose={(auctionId, action) => closeAuction.mutate({ auctionId, action })} /> : null}{tab === "users" ? <UsersPanel rows={queues.data?.users ?? []} currentId={auth.user.id} busy={setUserStatus.isPending} onToggle={(userId, status) => setUserStatus.mutate({ userId, status })} /> : null}{tab === "audit" ? <AuditPanel rows={queues.data?.audit ?? []} /> : null}</div></DashboardLayout>;
}

function Metric({ label, value, icon: Icon }: { label: string; value: number; icon: typeof Gavel }) { return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><Icon className="h-5 w-5 text-[#e98716]" /><p className="mt-4 text-3xl font-black text-[#172b4d]">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></section>; }
function ListingQueue({ rows, approve, reject, busy }: { rows: { listing: { id: number; title: string; city: string }; owner: string | null; ownerEmail: string | null }[]; approve: (id: number) => void; reject: (id: number) => void; busy: boolean }) { return <Panel title="إعلانات بانتظار المراجعة">{rows.length ? <div className="divide-y divide-slate-100">{rows.map(({ listing, owner, ownerEmail }) => <div key={listing.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-black text-[#172b4d]">{listing.title}</h3><p className="mt-1 text-xs text-slate-500">{owner || "مستخدم"} · {ownerEmail || ""} · {listing.city}</p></div><div className="flex gap-2"><Button onClick={() => approve(listing.id)} disabled={busy} className="rounded-xl bg-emerald-600 hover:bg-emerald-700">اعتماد</Button><Button variant="outline" onClick={() => reject(listing.id)} disabled={busy} className="rounded-xl border-red-200 text-red-700 hover:bg-red-50">رفض</Button></div></div>)}</div> : <Empty text="لا توجد إعلانات بانتظار المراجعة." />}</Panel>; }
function DocumentQueue({ rows, approve, reject, busy }: { rows: { document: { id: number; documentType: string; mimeType: string; url: string }; owner: string | null }[]; approve: (id: number) => void; reject: (id: number) => void; busy: boolean }) { return <Panel title="وثائق التوثيق">{rows.length ? <div className="divide-y divide-slate-100">{rows.map(({ document, owner }) => <div key={document.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-black text-[#172b4d]">{document.documentType === "identity" ? "وثيقة هوية" : document.documentType}</h3><p className="mt-1 text-xs text-slate-500">{owner || "مستخدم"} · {document.mimeType}</p><a href={document.url} target="_blank" rel="noreferrer" className="mt-2 inline-block text-xs font-black text-[#e98716]">عرض الملف</a></div><div className="flex gap-2"><Button onClick={() => approve(document.id)} disabled={busy} className="rounded-xl bg-emerald-600 hover:bg-emerald-700">اعتماد</Button><Button variant="outline" onClick={() => reject(document.id)} disabled={busy} className="rounded-xl border-red-200 text-red-700 hover:bg-red-50">رفض</Button></div></div>)}</div> : <Empty text="لا توجد وثائق بانتظار المراجعة." />}</Panel>; }
function AuctionPanel({ rows, selectedId, onSelect, bids, loadingBids, busy, onClose }: { rows: { auction: { id: number; status: string; endsAt: number; currentBid: string; minimumIncrement: string }; listing: { title: string; city: string } }[]; selectedId: number | null; onSelect: (id: number | null) => void; bids: { bid: { id: number; amount: string; createdAt: number }; bidder: string | null; bidderEmail: string | null }[]; loadingBids: boolean; busy: boolean; onClose: (id: number, action: "ended" | "cancelled") => void }) { const selected = rows.find((row) => row.auction.id === selectedId); return <div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><Panel title="إدارة المزادات">{rows.length ? <div className="divide-y divide-slate-100">{rows.map(({ auction, listing }) => <button key={auction.id} onClick={() => onSelect(auction.id)} className={`flex w-full items-center justify-between gap-3 p-5 text-right transition ${selectedId === auction.id ? "bg-[#fff5e5]" : "hover:bg-slate-50"}`}><div><h3 className="font-black text-[#172b4d]">{listing.title}</h3><p className="mt-1 text-xs text-slate-500">{listing.city} · ينتهي {dateTime.format(new Date(auction.endsAt))}</p></div><div className="text-left"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-black text-slate-600">{auction.status}</span><p className="mt-2 text-xs font-bold text-[#e98716]">{currency.format(decimalToNumber(auction.currentBid))} ر.ي</p></div></button>)}</div> : <Empty text="لا توجد مزادات مسجلة." />}</Panel><section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="font-black text-[#172b4d]">سجل المزايدات والإجراءات</h2>{selected ? <><p className="mt-2 text-sm text-slate-500">{selected.listing.title}</p>{["live", "scheduled"].includes(selected.auction.status) ? <div className="mt-4 flex gap-2"><Button disabled={busy} onClick={() => onClose(selected.auction.id, "ended")} className="rounded-xl bg-[#e98716] hover:bg-[#cf7814]">إنهاء واختيار الأعلى</Button><Button disabled={busy} variant="outline" onClick={() => onClose(selected.auction.id, "cancelled")} className="rounded-xl border-red-200 text-red-700 hover:bg-red-50">إلغاء المزاد</Button></div> : <p className="mt-4 rounded-xl bg-slate-100 p-3 text-sm text-slate-500">هذا المزاد مغلق ولا يمكن تعديل حالته.</p>}<div className="mt-5 border-t border-slate-100 pt-4">{loadingBids ? <p className="text-sm text-slate-500">جارٍ تحميل المزايدات...</p> : bids.length ? <div className="space-y-3">{bids.map(({ bid, bidder, bidderEmail }) => <div key={bid.id} className="flex justify-between rounded-xl bg-slate-50 p-3 text-sm"><span className="text-slate-600">{bidder || "مستخدم"}<small className="mr-1 text-slate-400">{bidderEmail || ""}</small></span><span className="font-black text-[#172b4d]">{currency.format(decimalToNumber(bid.amount))} ر.ي</span></div>)}</div> : <p className="text-sm text-slate-500">لا توجد مزايدات على هذا المزاد.</p>}</div></> : <Empty text="اختر مزادًا لعرض سجل المزايدات." />}</section></div>; }
function UsersPanel({ rows, currentId, busy, onToggle }: { rows: { id: number; name: string | null; email: string | null; openId: string; role: string; status: "active" | "suspended" }[]; currentId: number; busy: boolean; onToggle: (id: number, status: "active" | "suspended") => void }) { return <Panel title="إدارة المستخدمين"><div className="divide-y divide-slate-100">{rows.map((user) => <div key={user.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-black text-[#172b4d]">{user.name || "مستخدم بلا اسم"}</h3><p className="mt-1 text-xs text-slate-500">{user.email || user.openId} · {user.role === "admin" ? "مشرف" : "مستخدم"}</p></div><div className="flex items-center gap-3"><span className={`rounded-full px-2.5 py-1 text-xs font-black ${user.status === "active" ? "bg-emerald-50 text-emerald-700" : "bg-red-50 text-red-700"}`}>{user.status === "active" ? "نشط" : "معلق"}</span>{user.id !== currentId ? <Button variant="outline" onClick={() => onToggle(user.id, user.status === "active" ? "suspended" : "active")} disabled={busy} className="rounded-xl">{user.status === "active" ? "تعليق" : "تنشيط"}</Button> : null}</div></div>)}</div></Panel>; }
function AuditPanel({ rows }: { rows: { audit: { id: number; action: string; entityType: string; entityId: number | null; detail: string | null; createdAt: number }; actor: string | null }[] }) { return <Panel title="سجل العمليات">{rows.length ? <div className="divide-y divide-slate-100">{rows.map(({ audit, actor }) => <div key={audit.id} className="p-4"><p className="font-bold text-[#172b4d]">{audit.action}</p><p className="mt-1 text-xs text-slate-500">{actor || "النظام"} · {audit.entityType} #{audit.entityId || "—"} · {dateTime.format(new Date(audit.createdAt))}</p>{audit.detail ? <p className="mt-1 text-xs text-slate-500">{audit.detail}</p> : null}</div>)}</div> : <Empty text="لا توجد عمليات مسجلة بعد." />}</Panel>; }
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200"><div className="border-b border-slate-100 p-5"><h2 className="font-black text-[#172b4d]">{title}</h2></div>{children}</section>; }
function Empty({ text }: { text: string }) { return <p className="p-8 text-center text-sm text-slate-500">{text}</p>; }
