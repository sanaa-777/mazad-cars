# تدقيق الوصول وتجربة لوحة المفاتيح

راجعت الصفحات العامة والواجهات التفاعلية الأساسية في 23 أغسطس 2026. ركز التدقيق على بنية RTL، الوصول بلوحة المفاتيح، مرئية التركيز، التسميات، والحالات غير الناجحة. لا يغني هذا عن اختبار شامل بأدوات قارئ الشاشة مع مستخدمين حقيقيين قبل الإطلاق العام.

| الصفحة أو المكوّن | لوحة المفاتيح والتركيز | الدلالة والحالات | النتيجة |
| --- | --- | --- | --- |
| الصفحة الرئيسية `/` | روابط التنقل، البحث، وأزرار CTA قابلة للوصول بـ Tab وEnter مع حلقة تركيز مرئية. | هيكل `header` و`nav` و`main`، تحميل معلن، ورسالة خطأ بـ `role=alert`. | مكتمل للمسار الأساسي. |
| السوق `/listings` | حقول البحث والفلاتر والفرز والترقيم قابلة للاستخدام بلوحة المفاتيح. | تسميات مخفية للقارئات، مؤشرات تحميل ورسالة خطأ وحالة فراغ. | مكتمل للمسار الأساسي. |
| المزادات `/auctions` | بطاقات المزاد وروابطها قابلة للتركيز. | تحميل معلن، خطأ معلن، وحالة فراغ. | مكتمل للمسار الأساسي. |
| تفاصيل الإعلان `/listings/:id` | المفضلة، المراسلة، نموذج المزايدة وسجلها قابلة للوصول بالترتيب الطبيعي. | حالة تحميل، خطأ استعلام، وعدم وجود إعلان متميزة بوضوح. | مكتمل للمسار الأساسي. |
| إضافة إعلان `/listings/new` | تسلسل الحقول والأزرار متسق، ونوع الإعلان يستخدم `aria-pressed`. | `label` لكل حقل وحقول إلزامية وقيود رفع واضحة. | مكتمل للمسار الأساسي. |
| التنقل العام | عناصر الصفحة الحالية تستخدم `aria-current`، والتنقل السفلي والهيدر لهما أسماء وصول واضحة. | حلقات تركيز بلون برتقالي متباين. | مكتمل للمسار الأساسي. |

## قائمة اختبار يدوية قبل النشر

1. انتقل عبر كل صفحة بمفتاح Tab وShift+Tab وتأكد من أن موضع التركيز مرئي ولا يُحجب خلف التنقل السفلي.
2. نفذ أزرار البحث والإرسال والمزايدة بـ Enter أو Space، وتأكد من ظهور أي خطأ في قارئ الشاشة.
3. اختبر النموذج بمستعرض يدعم التحقق الأصلي وبتكبير 200% على شاشة جوال.
4. اختبر قارئ شاشة يدعم العربية في الصفحة الرئيسية، السوق، نموذج الإعلان، وتفاصيل الإعلان.
